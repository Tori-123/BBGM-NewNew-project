---
description: 后端实现约定
globs: backend/**/*
alwaysApply: false
---

# 后端

栈：Python + FastAPI（`backend/`）。`GET /health` 已有；业务路由按 `docs/schema.md` 增加，不要另起一套 path。

## 契约

Path、Method、JSON **必须**与 `docs/schema.md` 一致。

- Base URL：`/api/v1`。鉴权：Cookie Session，名 `scoop_session`。
- 成功：对象直接放 JSON 根（不要再包一层 `data`）。
- 错误：`{"error":{"code","message","fields":[{"field","message"}]}}`，状态码按 schema 表。
- id：UUID 字符串。时间：UTC ISO 8601 带 `Z`。列表：`items` `page` `page_size` `total`。
- 栏目枚举仅 `news` | `dorm_life` | `sports` | `events`。`status` 本版仅 `published`。
- 活动不是独立资源：`is_activity` + `starts_at` + `location`。
- 响应不要出现 `password`、`password_hash`、审计记录。公开帖作者只用 `author.id`、`author.display_name`。

要改形状：先改 `docs/schema.md`，再改后端，并同步前端。不要只在代码里加字段。

## 按 PRD 实现，不按想象加

需要则做：注册/登录/退出、会话、用户与帖子持久化（`DATABASE_URL`）、发帖时写审计（只写不公开查）、校验与 `503 storage_unavailable`。

不需要则不要加：SSO、先审后发、推荐、评论、改删帖、检索、Spotify、向量库、微服务。无第三方 API。

密钥从环境变量读（`SESSION_SECRET`、`DATABASE_URL`）。不要提交 `.env`。

允许为 Must Have 写 pytest。不要禁止测试，也不要强制每次改动必须先 TDD。
